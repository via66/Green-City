
[
{% for feature in features %}
	{
		"fields": { 
			"latitude": {{feature.latitude}},
			"longitude": {{feature.longitude}},
			"name": "{{feature.name}}"

		}
	}
	{% if not forloop.last %}
		,
	{% endif %}
{% endfor %}
]